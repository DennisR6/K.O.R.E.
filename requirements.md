# Slipstrike Requirements Ledger

## Purpose

This ledger connects the reconciled implementation inventory in `TODO.md` and
the product intent in `gdd.md` to the commit-sized work in `step-by-step.md`.
It prevents a feature from being considered complete solely because a prototype,
commented test, or editor-only data exists.

`Current evidence` names active tests or verified runtime behavior. `Required
evidence` is the acceptance coverage that must exist before the requirement can
be marked complete in `TODO.md` and `step-by-step.md`.

## Product Decisions

| Decision | Agreed behavior | Delivery section |
| --- | --- | --- |
| Team composition | One player controls a configurable number of figures against another player with a configurable number of figures. | [Match Rules](step-by-step.md#3-match-rules-and-round-lifecycle) |
| Item economy | A game mode may configure fixed loadouts, map pickups, and deterministic seeded draws. | [Items](step-by-step.md#5-item-framework-and-official-items) |
| Desktop and mobile | Desktop uses Tauri; mobile ships first as responsive, offline-capable web. | [Platforms](step-by-step.md#9-desktop-mobile-and-discord) |
| Discord | Presence and invites are optional at runtime and configured through environment values, never committed credentials. | [Platforms](step-by-step.md#9-desktop-mobile-and-discord) |
| Mod sharing | No separate workshop backend. Validated `game.handler.toSettings()` exports and authoritative server settings are the sharing path. | [Persistence](step-by-step.md#8-persistence-sharing-and-replays) |

## Traceability Matrix

| ID | Requirement source | Delivery checklist | Current evidence | Required evidence |
| --- | --- | --- | --- | --- |
| R-01 | TODO Grundprinzip and Phase 1; GDD 2 and 13 | [Baseline and Architecture](step-by-step.md#1-baseline-and-architecture) | `tests/engine_test.ts` covers handler behavior; `tests/turn_system.test.ts` covers active-team persistence; `tests/seeded_random.test.ts` covers repeatable random sequences; `tests/rule_interpreter.test.ts` covers data-defined phase advancement. | Fixed-tick browser simulation and tests proving engine simulation is independent of game rules. |
| R-02 | TODO Phase 2 figure state and shot mechanics; GDD 5 | [Physics](step-by-step.md#2-deterministic-physics-and-arena-safety) | `tests/ui_input.test.ts` covers drag angle/power; `tests/physics_test.ts` covers vector math and circle collision; `tests/deterministic_turn.test.ts` snapshots a real-map turn; `tests/authoritative_game.test.ts` round-trips player rotation and angular velocity; `tests/rotation_direction.test.ts` covers cardinal forward vectors; `tests/player_mass_validation.test.ts` rejects invalid player mass. | Shared forward-vector impulse tests and force-modifier tests. |
| R-03 | TODO Phase 2 movement and drift; GDD 5 | [Physics](step-by-step.md#2-deterministic-physics-and-arena-safety) | `tests/effect_test.ts` covers movement/friction and stop behavior; `tests/map_drift_settings.test.ts` covers valid and invalid map drift factors; `tests/drift_steering.test.ts` covers fixed-frame speed-preserving steering and stop-threshold behavior; `tests/full_physics_fixture.test.ts` snapshots seeded drift and friction. | Map friction injection. |
| R-04 | TODO Phase 3; GDD 5 death and collisions | [Physics](step-by-step.md#2-deterministic-physics-and-arena-safety) | Circle/circle and circle/rectangle behavior has physics tests; `tests/rectangle_collision.test.ts` covers rectangle dispatch; `tests/line_collision.test.ts` covers finite-segment detection and reflection; `tests/physics_enabled_collision.test.ts` covers disabled collision participants; `tests/circle_rendering.test.ts` aligns circle rendering; `tests/arena_containment.test.ts` covers inferred outer containment; `tests/boundary_death.test.ts` covers deterministic elimination; `tests/dead_entities.test.ts` covers dead-player exclusions and snapshot preservation; `tests/full_physics_fixture.test.ts` snapshots seeded player collision and boundary death; `tests/setting_effect.test.ts` covers death circles. | Full map-level collision and boundary fixtures. |
| R-05 | TODO Phase 4; GDD 4 and 8 | [Baseline and Architecture](step-by-step.md#1-baseline-and-architecture) and [Match Rules](step-by-step.md#3-match-rules-and-round-lifecycle) | `tests/rule_types.test.ts` covers serializable rule contracts; `tests/rule_interpreter.test.ts` covers item allowance and required staged-shot transitions; `tests/ui_input.test.ts` covers simulation, playback, and match-end input locks; `tests/rule_state_snapshot.test.ts` covers persisted rule progress and match results; `tests/winning_system.test.ts` covers configurable last-team-standing evaluation; `tests/local_rematch.test.ts` covers configured local rematch restoration; `tests/engine_emitter.test.ts` covers local rule-driven progression; `tests/turn_system.test.ts` and `tests/team_figure_counts.test.ts` cover configured player/figure layouts; `tests/authoritative_game.test.ts` covers rule-phase protocol synchronization, authoritative turn ownership, and rematch reset. | Completed-match lifecycle integration tests. |
| R-06 | TODO Grundprinzip, Phase 5, and Phase 6; GDD 6, 7, and 12 | [Data Contracts](step-by-step.md#4-data-contracts-mods-and-maps) | `tests/document_versions.test.ts` covers version-one migration; `tests/game_settings_validation.test.ts` covers malformed settings rejection; `tests/mod_security.test.ts` covers declarative mod security, prototype pollution protection, and executable key rejection. | Rejected untrusted data at load boundaries and effect-whitelist tests. |
| R-07 | TODO Phase 5; GDD 6 official items | [Items](step-by-step.md#5-item-framework-and-official-items) | `tests/item_types.test.ts` covers versioned item document schemas, inventory items, pickups, and structural validation; `tests/item_validator.test.ts` covers registered-effect whitelisting, declarative JSON-only values, executable-field rejection, and field allowlists; `tests/item_loader.test.ts` covers the shared built-in/local-mod validation path, duplicate IDs, and defensive copies; `tests/item_economy_settings.test.ts` covers serializable per-mode fixed loadouts, map pickups, and seeded draw settings; `tests/item_inventory.test.ts` covers fixed-loadout initialization, consumption limits, reset, and snapshot restoration; `tests/item_draws.test.ts` covers deterministic draws and resumed serialized draw state; `tests/item_pickups.test.ts` covers active-team pickup collection, per-turn limits, and snapshot restoration; `tests/item_targets.test.ts` covers entity relationship/range checks and finite in-world position/zone validation; `tests/item_authorization.test.ts` covers item-phase/team authorization, inventory consumption, and authoritative packet broadcasts; `tests/item_emitter.test.ts` covers local emission, network request serialization, and client reconciliation of authoritative item state; `tests/modify_force.test.ts` covers serializable force multipliers and deterministic stacking; `tests/modify_rotation.test.ts` covers serializable additive rotation changes and angle normalization; `tests/lock_rotation.test.ts` covers serializable turn countdown and expiration; `tests/apply_torque.test.ts` covers serializable torque and deterministic angular updates; `tests/spawn_trigger.test.ts` covers deterministic trigger countdown and snapshot restoration; `tests/delayed_effect.test.ts` covers fixed-tick scheduling and restored countdown state; `tests/shield.test.ts` covers damage absorption, collision blocking, and serialized shield state; `tests/freeze.test.ts` covers movement reduction, expiration, and serialized duration; `tests/swap_position.test.ts` covers atomic swaps, teleport destinations, serialization, and invalid target rejection; `tests/temporary_wall.test.ts` covers wall spawning, lifecycle cleanup, and serialized active state; `tests/anker_item.test.ts` covers validated built-in Anker data and deterministic force reduction; `tests/ghost_mode_item.test.ts` covers validated Durchlässigkeit data, collision filtering, expiration, and state restoration; `tests/magnet_item.test.ts` covers validated Magnet data, attraction/repulsion, range handling, and serialization; `tests/falltuer_item.test.ts` covers position-targeted Falltür data and kill-zone trigger behavior; `tests/power_dash_item.test.ts` covers validated force-boost data and behavior; `tests/delayed_mine_item.test.ts` covers delayed triggering and deterministic repelling explosion; `tests/mini_wall_item.test.ts` covers validated portable wall data, spawning, cleanup, and placement validation; `tests/freeze_shot_item.test.ts` covers validated enemy targeting, temporary slowing, expiration, and serialization; `tests/switch_item.test.ts` covers validated ally targeting, atomic position swaps, and inactive-target rejection. | Every official item and mixed-effect regression tests. |
| R-08 | TODO Phase 6; GDD 7 and 12 | [Data Contracts](step-by-step.md#4-data-contracts-mods-and-maps) | `tests/cue_clash_map.test.ts` covers a scaled canonical Cue Clash map and real shot; `tests/frostbite_arena_map.test.ts` covers Frostbite's independent world size, ice friction, extreme drift, and fixed-frame behavior; `tests/magma_cradle_map.test.ts` covers scaled validated force and kill-zone adapters plus collision elimination; `tests/map_document.test.ts` covers canonical map physics, geometry, spawns, hazards, and metadata; `tests/map_loader.test.ts` covers map conversion and unsupported hazards; `tests/hazard_registry.test.ts` covers hazard registry validation; `tests/force_hazard.test.ts`, `tests/rotation_hazard.test.ts`, `tests/kill_zone_hazard.test.ts`, `tests/slow_zone_hazard.test.ts`, and `tests/delayed_hazard.test.ts` cover deterministic hazards; `tests/setting_effect.test.ts` verifies map-configured death circles only; `tests/editor_map_document.test.ts` validates versioned editor exports and rejects malformed shapes, numbers, and collections; `tests/editor_map_conversion.test.ts` covers conversion, rectangular collision effects, and unsupported editor features; `tests/editor_dom_rendering.test.ts` verifies imported strings are assigned as text or control values without creating markup; `tests/editor_module_graph.test.ts` loads editor ESM modules and verifies the cross-module item UI exports and mode-state binding; `tests/editor_ids.test.ts` verifies unique static editor IDs and separate sidebar/editor hazard-add selectors; `tests/editor_draft.test.ts` covers validated temporary draft storage and safe restoration; `tests/editor_preview.test.ts` covers configured preview URL derivation and stable popup settings. | Editor-export conversion tests. |
| R-09 | TODO Phase 7 and Phase 9; GDD 3, 10, and 11 | [Input and UI](step-by-step.md#6-input-ui-and-accessibility) | `tests/ui_input.test.ts`, `tests/direction_arrow.test.ts`, and `tests/scaling_test.ts` cover current mouse, arrow, and scaling behavior. | Configurable actions, aim/charge separation, item UI, controller/touch input, and viewport accessibility tests. |
| R-10 | TODO Phase 8; GDD 8 and 9 | [AI](step-by-step.md#7-ai-and-play-modes) | `tests/ai_settings.test.ts` validates serializable AI settings; `tests/ai_emitter.test.ts` covers shared AI turn emitter interface; `tests/easy_ai.test.ts` covers deterministic easy AI; `tests/medium_ai.test.ts` covers heuristic medium AI; `tests/hard_ai.test.ts` covers simulation hard AI; `tests/versus_ai.test.ts` covers versus AI game mode; `tests/authoritative_ai.test.ts` covers authoritative AI matches and replay verification. | Seeded easy AI, heuristic medium AI, bounded-search hard AI, and authoritative complete-match tests. |
| R-11 | TODO Phase 10 savegames and Optional replays; GDD 2 and 12 | [Persistence](step-by-step.md#8-persistence-sharing-and-replays) | `tests/authoritative_game.test.ts` covers SQLite persistence, eviction, and reconnect; no replay playback test exists. | Local save-slot tests, validated settings export/import, replay record/playback final-snapshot tests, and replay UI error coverage. |
| R-12 | TODO Phase 10; GDD 3 and 15 | [Platforms](step-by-step.md#9-desktop-mobile-and-discord) | `tests/desktop_packaging.test.ts` and `tests/tauri_config.test.ts` verify the Tauri project scaffold and `bun run desktop:build` native build artifacts; `tests/mobile_manifest.test.ts` and `tests/mobile_offline.test.ts` verify PWA/mobile offline paths. | Tauri build smoke test, installable offline mobile-web test, and mobile reconnect/touch smoke test. |
| R-13 | TODO Phase 11; GDD 3 and 14 | [Platforms](step-by-step.md#9-desktop-mobile-and-discord) | No Discord runtime or dependency exists. | Disabled-config no-op test, mocked Rich Presence state tests, and validated join/invite tests. |
| R-14 | TODO optional workshop decision; GDD 12 | [Persistence](step-by-step.md#8-persistence-sharing-and-replays) | Scope decision recorded in `TODO.md`; no workshop is required. | Validated settings export and server-distributed settings tests provide the agreed sharing mechanism. |
| R-15 | All TODO/GDD requirements | [Final Hardening](step-by-step.md#10-final-hardening-and-release-readiness), [Cross-System Validation](step-by-step.md#11-cross-system-validation), and [Defect Hardening And Release Qualification](step-by-step.md#12-defect-hardening-and-release-candidate-qualification) | `bun test` runs 425 tests across 157 files cleanly (2221 assertions); `tests/local_match_lifecycle.integration.test.ts` and `tests/e2e_network_match.test.ts` cover complete match lifecycles; `tests/input_fuzz.test.ts`, `tests/mod_security.test.ts`, `tests/cleanup.test.ts`, and `tests/documentation.test.ts` cover security and docs; the Section 11 cross-system validation suite proves snapshot isolation (`tests/handler_snapshot_isolation.test.ts`, `tests/simulate_turn_isolation.test.ts`, `tests/hard_ai_snapshot_validation.test.ts`, `tests/parallel_engine_instances.test.ts`), replay lifecycle (`tests/ai_replay_lifecycle.test.ts`), persistence continuation (`tests/persisted_match_continuation.test.ts`), winning composition (`tests/winning_lifecycle_validation.test.ts`), item-effect snapshot continuity (`tests/item_effect_snapshot_validation.test.ts`), and uniform action-path rejection (`tests/action_path_consistency.test.ts`); `tests/cross_system_validation_smoke.test.ts` references every section 11 file; the Section 12 suite hardens the effect factory (`tests/effect_factory_roundtrip.test.ts`), gates gameplay systems on match completion (`tests/match_completion_gate.test.ts`), models explicit match status results (`tests/match_status_model.test.ts`, `tests/winner_state_unification.test.ts`), purifies settings export (`tests/settings_export_purity.test.ts`), restores replay rule-state orchestration (`tests/replay_rule_state_orchestration.test.ts`), ships the deterministic AI-vs-AI fuzz suite (`tests/ai_match_fuzz.test.ts` plus `tests/support/aiMatchFuzz.ts`, with `RC_GAME_COUNT`-controlled smoke/RC/soak runs in package.json), and records the 24-point qualification in `tests/release_candidate_gate.test.ts` and `docs/release-verification.md`. | Local/network end-to-end matches, malformed-input coverage, mod-boundary security audit, clean release verification, and release documentation. |

| R-16 | Physics solver contract | [Physics Qualification](step-by-step.md#13-physics-solver-hardening-and-continuous-collision-qualification) | `tests/physics_qualification_gate.test.ts` links the Section 13 contact, CCD, energy, lifecycle, snapshot, fuzz, and performance evidence; `bun run test:physics-fuzz:rc` and `bun run test:physics-fuzz:soak` qualify 5,000 and 25,000 seeded cases. | Section 13 complete. |

| R-17 | Section 15 gameplay release qualification | [Gameplay Qualification](step-by-step.md#15-gameplay-qualification-and-human-playtest-validation) | `tests/gameplay_release_gate.test.ts` gates the matrix, softlock, pacing, fairness, agency, item, vertical-slice, package, and human-playtest evidence. The automated record is explicit, but the final status remains `BLOCKED / NOT QUALIFIED` until an external tester completes the two-match protocol. | Complete automated evidence record and actual human playtest evidence. |

| R-18 | Section 16 real-browser gameplay verification | [Browser Verification](step-by-step.md#16-real-browser-gameplay-verification) | `tests/browser/browser_release_gate.test.ts` gates the Section 16 evidence: the harness (`tests/browser/browserHarness.ts`) builds the generated bundle and manages the Bun server lifecycle; startup/menu, local-turn, full match flow (item panel, deterministic kill, result overlay, rematch, menu exit), failure diagnostics, and the console policy are covered by the real-browser E2E suite; `bun run test:browser:smoke` (9 tests) and `bun run test:browser:full` (15 tests) both PASS and are wired into the `browser` CI job. Browser-playable status is recorded in `docs/release-verification.md` as PASS, while the Section 15 gameplay release record remains `BLOCKED / NOT QUALIFIED` pending the external human session. | Smoke and complete local-match flow must both pass headless in CI before a release candidate is declared browser-playable. |
| R-19 | Section 17 qualified map production and verification | [Map Production and Verification](step-by-step.md#17-qualified-map-production-and-verification) | `tests/map_release_gate.test.ts` gates the Section 17 evidence: the design contract (`docs/map-design-contract.md`), the status ledger (`docs/map-qualification-report.md`), the authoritative inventory (`src/content/mapCatalog.ts`), the qualification harness (`tests/support/mapQualification.ts`), the symmetric duel map, structure-control map, hazard-control map, the complete map matrix (`tests/shipped_map_matrix.test.ts`), browser verification (`tests/browser/map_catalog.e2e.test.ts`), and the playtest readiness packet (`tests/map_playtest_readiness.test.ts`). Map-level human qualification remains pending (`PENDING`) while technical and browser qualifications pass. | All candidates must have explicit, evidence-backed statuses in the ledger, and only qualified maps are exposed as browser-available. |
| R-20 | Database-backed immutable map definitions | [Database-Backed Immutable Map Definitions Checklist](step-by-step.md#database-backed-immutable-map-definitions-checklist) | `tests/map_store.test.ts` verifies UUID-only immutable `MapDocument` insertion, canonical content hashes, defensive reads, approval, retirement, duplicate rejection, and malformed-document rejection in SQLite. `tests/map_repository.test.ts` verifies that only approved revisions are converted by the server-side canonical loader. | Authoritative matchmaking selection, immutable match map references, cache refresh, and authority-rejection coverage. |

## Update Rules

- Add a concrete test path to `Current evidence` when a checklist item is
  completed; do not cite commented or assertion-free tests as proof.
- If implementation changes a requirement interpretation, update the relevant
  TODO acceptance criterion, this ledger, and the checklist in one commit.
- Add a new ledger row before implementing a product requirement not represented
  by TODO, the GDD, or an existing row.
